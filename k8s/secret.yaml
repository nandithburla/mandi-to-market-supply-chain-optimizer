apiVersion: v1
kind: Secret
metadata:
  name: mandi-to-market-secrets
  labels:
    app: mandi-to-market
type: Opaque
stringData:
  # Replace with actual OpenRouter API key before deploying to AWS EKS
  OPENROUTER_API_KEY: "your_openrouter_api_key_here"
